# CLAUDE.md — PlanVisualizer Session Instructions

> Auto-generated by PlanVisualizer install script. Edit to fit your project.

---

## Mandatory Session Startup

1. Read `AGENTS.md` in full before writing any code or using any tools.
2. Read `MEMORY.md` (if present) for cross-session context.
3. Read `progress.md` to understand where the last session ended.
4. Read `docs/ID_REGISTRY.md` (if present) before creating any new artefact ID.

---

## PlanVisualizer Dashboard

- **Entry point:** `node tools/generate-plan.js`
- **Output:** `docs/plan-status.html`
- **Config:** `plan-visualizer.config.json`
- **Format guide:** `plan_visualizer.md` (authoritative spec for all parsed files)

Run `npm run plan:generate` to regenerate the dashboard after making changes to
`RELEASE_PLAN.md`, `TEST_CASES.md`, `BUGS.md`, `AI_COST_LOG.md`, or `progress.md`.

---

## Key Protocols

| Protocol           | Rule                                                              |
| ------------------ | ----------------------------------------------------------------- |
| Unit Testing       | ≥80% coverage; all tests pass before any commit                   |
| Git Workflow       | `feature/*` → `develop` (PR) → `main` (PR)                       |
| Session Close      | Update `progress.md` and `MEMORY.md` before ending               |

---

## Commit Message Format

```
[TYPE] SHORT-ID: Short imperative description (max 72 chars)
```

Types: `feat`, `fix`, `test`, `docs`, `refactor`, `chore`, `style`, `perf`
