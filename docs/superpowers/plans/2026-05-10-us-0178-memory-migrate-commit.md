# US-0178 Automated Memory Migration Commit Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add `tools/memory.js migrate-commit` subcommand that runs the memory migration, patches CLAUDE.md, runs tests, commits, and optionally pushes + creates a PR — making PR B reproducible and gated.

**Architecture:** Two new lib modules (`memory-claude-md-patcher.js` and `memory-commit-orchestrator.js`) under `tools/lib/`. The orchestrator imports the existing `migrateMemory`, `validateMemory`, and the new patcher. `tools/memory.js` gains flag parsing for `--push/--pr/--no-test` and a `migrate-commit` dispatch branch. No changes to the migration logic itself (US-0175).

**Tech Stack:** Node.js 18+, Jest 30, `child_process.execFileSync`, `gh` CLI (optional, only for `--pr`)

---

## File Map

| File                                            | Change                                                                             |
| ----------------------------------------------- | ---------------------------------------------------------------------------------- |
| `tools/lib/memory-claude-md-patcher.js`         | New — AST-style CLAUDE.md patcher                                                  |
| `tools/lib/memory-commit-orchestrator.js`       | New — 7-step pipeline                                                              |
| `tools/memory.js`                               | Modify — add `--push/--pr/--no-test` to `parseArgs`, add `migrate-commit` dispatch |
| `package.json`                                  | Modify — add `memory:migrate-commit` npm script                                    |
| `tests/unit/memory-claude-md-patcher.test.js`   | New — 6 patcher tests                                                              |
| `tests/unit/memory-commit-orchestrator.test.js` | New — 8 pipeline tests                                                             |

---

### Task 1: `memory-claude-md-patcher.js` — CLAUDE.md AST patcher

**Files:**

- Create: `tools/lib/memory-claude-md-patcher.js`
- Create: `tests/unit/memory-claude-md-patcher.test.js`

The patcher takes CLAUDE.md text and returns patched text. It locates named sections by `## ` heading, makes deterministic insertions, and throws (does not return an error value) on any condition that requires manual reconciliation.

- [ ] **Step 1: Write the failing tests**

```js
// tests/unit/memory-claude-md-patcher.test.js
'use strict';
const { patchClaudeMd } = require('../../tools/lib/memory-claude-md-patcher');

const FIXTURE = [
  '# CLAUDE.md',
  '',
  '## Mandatory Session Startup',
  '',
  '1. Read `AGENTS.md` in full before writing any code or using any tools.',
  '2. Read `MEMORY.md` and all linked topic files.',
  '3. Read `PROMPT_LOG.md` to understand the prompt history.',
  '4. Check `docs/ID_REGISTRY.md` before creating any new artefact.',
  '',
  '## Session Close Checklist',
  '',
  '- [ ] All changes committed to feature branch',
  '- [ ] `MEMORY.md` updated with new learnings',
  '- [ ] `progress.md` updated',
].join('\n');

const IDEMPOTENT_FIXTURE = FIXTURE.replace(
  '2. Read `MEMORY.md` and all linked topic files.',
  '2. Read `MEMORY.md` and all linked topic files.\n3. Memory files live in `docs/memory/{topics,sessions,snapshots}/`. Read `MEMORY.md` (compact index) at session start; read specific topic files when their topic is relevant to the current task. Do not edit `MEMORY.md` directly — it is auto-regenerated by `generate-plan.js`.',
);

describe('patchClaudeMd', () => {
  test('inserts item 3 after "2. Read MEMORY.md" and renumbers subsequent items', () => {
    const { text, changed } = patchClaudeMd(FIXTURE);
    expect(changed).toBe(true);
    const lines = text.split('\n');
    const item3Idx = lines.findIndex((l) => l.startsWith('3. Memory files live'));
    expect(item3Idx).toBeGreaterThan(-1);
    // Old item 3 (PROMPT_LOG) should now be item 4
    const item4Line = lines[item3Idx + 1];
    expect(item4Line).toMatch(/^4\. Read `PROMPT_LOG/);
    // Old item 4 (ID_REGISTRY) should now be item 5
    const item5Line = lines.find((l) => l.match(/^5\./));
    expect(item5Line).toMatch(/^5\. Check `docs\/ID_REGISTRY/);
  });

  test('replaces MEMORY.md checklist line with 4 nested lines', () => {
    const { text } = patchClaudeMd(FIXTURE);
    expect(text).toContain('Memory files updated in `docs/memory/{topics,sessions,snapshots}/`');
    expect(text).not.toContain('`MEMORY.md` updated with new learnings');
    // 4 replacement lines should be present
    const lines = text.split('\n');
    const memoryIdx = lines.findIndex((l) => l.includes('Memory files updated'));
    expect(lines[memoryIdx + 1]).toContain('session learnings');
    expect(lines[memoryIdx + 2]).toContain('stable knowledge');
    expect(lines[memoryIdx + 3]).toContain('Do NOT edit');
  });

  test('is idempotent — returns changed:false if already patched', () => {
    const { changed } = patchClaudeMd(IDEMPOTENT_FIXTURE);
    expect(changed).toBe(false);
  });

  test('idempotent text is unchanged', () => {
    const { text } = patchClaudeMd(IDEMPOTENT_FIXTURE);
    expect(text).toBe(IDEMPOTENT_FIXTURE);
  });

  test('throws if "2. Read MEMORY.md" line not found', () => {
    const broken = FIXTURE.replace('2. Read `MEMORY.md`', '2. Read `NOTES.md`');
    expect(() => patchClaudeMd(broken)).toThrow(/Mandatory Session Startup/);
  });

  test('throws if checklist line does not contain "updated with new learnings"', () => {
    const broken = FIXTURE.replace('`MEMORY.md` updated with new learnings', '`MEMORY.md` updated');
    expect(() => patchClaudeMd(broken)).toThrow(/manual reconciliation required/);
  });
});
```

- [ ] **Step 2: Run to confirm failure**

```bash
cd /Users/Kamal_Syed/Projects/PlanVisualizer
npx jest tests/unit/memory-claude-md-patcher.test.js --no-coverage 2>&1 | tail -5
```

Expected: `Cannot find module '../../tools/lib/memory-claude-md-patcher'`

- [ ] **Step 3: Create `tools/lib/memory-claude-md-patcher.js`**

```js
'use strict';

const IDEMPOTENCY_MARKER = 'Memory files live in `docs/memory/{topics,sessions,snapshots}/`';

const NEW_ITEM_3 =
  '3. Memory files live in `docs/memory/{topics,sessions,snapshots}/`. Read `MEMORY.md` (compact index) at session start; read specific topic files when their topic is relevant to the current task. Do not edit `MEMORY.md` directly — it is auto-regenerated by `generate-plan.js`.';

const CHECKLIST_REPLACEMENT = [
  '- [ ] Memory files updated in `docs/memory/{topics,sessions,snapshots}/`:',
  '- [ ]   - For session learnings: `docs/memory/sessions/YYYY-MM-DD-<short-slug>.md` with `# <Title> (Session N, YYYY-MM-DD)` heading',
  '- [ ]   - For new stable knowledge: `docs/memory/topics/<topic>.md` (no date prefix in filename)',
  '- [ ]   - Do NOT edit `MEMORY.md` directly — it is auto-regenerated by `generate-plan.js`',
];

/**
 * Patch CLAUDE.md text with memory layout instructions.
 *
 * @param {string} text - Raw CLAUDE.md content
 * @returns {{ text: string, changed: boolean }}
 * @throws {Error} if patch targets not found or checklist has been customised
 */
function patchClaudeMd(text) {
  if (text.includes(IDEMPOTENCY_MARKER)) {
    return { text, changed: false };
  }

  const lines = text.split('\n');

  // Patch 1: Find "2. Read `MEMORY.md`..." and insert new item 3 after it.
  let insertIdx = -1;
  for (let i = 0; i < lines.length; i++) {
    if (/^2\.\s+Read `MEMORY\.md`/.test(lines[i])) {
      insertIdx = i;
      break;
    }
  }
  if (insertIdx === -1) {
    throw new Error(
      'CLAUDE.md Mandatory Session Startup: could not find "2. Read `MEMORY.md`" line — manual reconciliation required',
    );
  }

  lines.splice(insertIdx + 1, 0, NEW_ITEM_3);

  // Renumber subsequent numbered list items (+1 each).
  for (let i = insertIdx + 2; i < lines.length; i++) {
    const m = lines[i].match(/^(\d+)(\.\s+.*)/);
    if (m) lines[i] = `${parseInt(m[1], 10) + 1}${m[2]}`;
  }

  // Patch 2: Find and replace the MEMORY.md checklist checkbox.
  let checklistIdx = -1;
  for (let i = 0; i < lines.length; i++) {
    if (lines[i].startsWith('- [') && lines[i].includes('updated with new learnings')) {
      checklistIdx = i;
      break;
    }
  }
  if (checklistIdx === -1) {
    throw new Error(
      'CLAUDE.md Session Close Checklist: could not find "`MEMORY.md` updated with new learnings" checkbox — manual reconciliation required',
    );
  }

  lines.splice(checklistIdx, 1, ...CHECKLIST_REPLACEMENT);

  return { text: lines.join('\n'), changed: true };
}

module.exports = { patchClaudeMd };
```

- [ ] **Step 4: Run tests to confirm they pass**

```bash
npx jest tests/unit/memory-claude-md-patcher.test.js --no-coverage 2>&1 | tail -5
```

Expected: 6 tests pass.

- [ ] **Step 5: Commit**

```bash
cd /Users/Kamal_Syed/Projects/PlanVisualizer
git add tools/lib/memory-claude-md-patcher.js tests/unit/memory-claude-md-patcher.test.js
git commit -m "feat(US-0178): add memory-claude-md-patcher.js — AST-style CLAUDE.md patcher"
```

---

### Task 2: `memory-commit-orchestrator.js` — 7-step pipeline

**Files:**

- Create: `tools/lib/memory-commit-orchestrator.js`
- Create: `tests/unit/memory-commit-orchestrator.test.js`

The orchestrator runs the migration pipeline. Tests mock `child_process.execFileSync`, `migrateMemory`, and `validateMemory` to avoid real side effects.

- [ ] **Step 1: Write the failing tests**

```js
// tests/unit/memory-commit-orchestrator.test.js
'use strict';
const fs = require('fs');
const path = require('path');
const os = require('os');

// Mocks must be declared before requires
jest.mock('child_process');
jest.mock('../../tools/lib/memory-migrator');
jest.mock('../../tools/lib/memory-validator');

const { execFileSync } = require('child_process');
const { migrateMemory } = require('../../tools/lib/memory-migrator');
const { validateMemory } = require('../../tools/lib/memory-validator');
const { runMigrateCommit } = require('../../tools/lib/memory-commit-orchestrator');

function setupRoot(opts = {}) {
  const tmp = fs.mkdtempSync(path.join(os.tmpdir(), 'mco-'));
  fs.writeFileSync(path.join(tmp, 'MEMORY.md'), '# MEMORY.md\n\n## Some Section\n\nbody\n');
  if (!opts.skipClaude) {
    fs.writeFileSync(
      path.join(tmp, 'CLAUDE.md'),
      [
        '# CLAUDE.md',
        '',
        '## Mandatory Session Startup',
        '',
        '1. Read `AGENTS.md`.',
        '2. Read `MEMORY.md` and all linked topic files.',
        '3. Read `PROMPT_LOG.md`.',
        '',
        '## Session Close Checklist',
        '',
        '- [ ] All committed',
        '- [ ] `MEMORY.md` updated with new learnings',
      ].join('\n'),
    );
  }
  fs.writeFileSync(
    path.join(tmp, 'docs', 'LESSONS.md').replace(/.*/, () => {
      fs.mkdirSync(path.join(tmp, 'docs'), { recursive: true });
      return path.join(tmp, 'docs', 'LESSONS.md');
    }),
    '# LESSONS.md\n',
  );
  fs.mkdirSync(path.join(tmp, 'docs'), { recursive: true });
  fs.writeFileSync(path.join(tmp, 'docs', 'LESSONS.md'), '# LESSONS.md\n');
  fs.writeFileSync(path.join(tmp, 'docs', 'ID_REGISTRY.md'), '| Lesson | L-0055 | L-0054 |\n');
  return tmp;
}

beforeEach(() => {
  jest.clearAllMocks();
  // Default: clean tree, feature branch, migration succeeds, tests pass, validate ok
  execFileSync.mockImplementation((cmd, args) => {
    if (cmd === 'git') {
      if (args.includes('status')) return '';
      if (args.includes('symbolic-ref')) return 'feature/test-branch\n';
      if (args.includes('commit')) return '';
      if (args.includes('push')) return '';
    }
    if (cmd === 'npx') return '';
    if (cmd === 'gh') return JSON.stringify([{ number: 995 }]);
    return '';
  });
  migrateMemory.mockReturnValue({
    skipped: false,
    topicFiles: [{ category: 'topics' }, { category: 'sessions' }],
    archiveOps: [{}],
    lessonOrphans: [],
  });
  validateMemory.mockReturnValue({ ok: true, diff: '' });
});

describe('runMigrateCommit pre-flight', () => {
  test('aborts on dirty working tree', async () => {
    execFileSync.mockImplementation((cmd, args) => {
      if (cmd === 'git' && args.includes('status')) return 'M tools/foo.js\n';
      return '';
    });
    const tmp = setupRoot();
    const code = runMigrateCommit({ root: tmp });
    expect(code).toBe(1);
    expect(migrateMemory).not.toHaveBeenCalled();
    fs.rmSync(tmp, { recursive: true, force: true });
  });

  test('aborts on develop branch', async () => {
    execFileSync.mockImplementation((cmd, args) => {
      if (cmd === 'git' && args.includes('status')) return '';
      if (cmd === 'git' && args.includes('symbolic-ref')) return 'develop\n';
      return '';
    });
    const tmp = setupRoot();
    const code = runMigrateCommit({ root: tmp });
    expect(code).toBe(1);
    expect(migrateMemory).not.toHaveBeenCalled();
    fs.rmSync(tmp, { recursive: true, force: true });
  });

  test('aborts if topics/ already exists and non-empty without --force', async () => {
    const tmp = setupRoot();
    fs.mkdirSync(path.join(tmp, 'docs/memory/topics'), { recursive: true });
    fs.writeFileSync(path.join(tmp, 'docs/memory/topics/x.md'), '# X\n');
    const code = runMigrateCommit({ root: tmp });
    expect(code).toBe(1);
    expect(migrateMemory).not.toHaveBeenCalled();
    fs.rmSync(tmp, { recursive: true, force: true });
  });
});

describe('runMigrateCommit pipeline', () => {
  test('aborts when migrateMemory throws', async () => {
    migrateMemory.mockImplementation(() => {
      throw new Error('migrate failed');
    });
    const tmp = setupRoot();
    const code = runMigrateCommit({ root: tmp });
    expect(code).toBe(1);
    fs.rmSync(tmp, { recursive: true, force: true });
  });

  test('aborts when jest exits non-zero', async () => {
    execFileSync.mockImplementation((cmd, args) => {
      if (cmd === 'git' && args.includes('status')) return '';
      if (cmd === 'git' && args.includes('symbolic-ref')) return 'feature/test\n';
      if (cmd === 'npx' && args.includes('jest')) {
        const e = new Error('jest failed');
        e.status = 1;
        throw e;
      }
      return '';
    });
    const tmp = setupRoot();
    const code = runMigrateCommit({ root: tmp });
    expect(code).toBe(1);
    fs.rmSync(tmp, { recursive: true, force: true });
  });

  test('aborts when validate returns not ok', async () => {
    validateMemory.mockReturnValue({ ok: false, diff: '--- MEMORY.md' });
    const tmp = setupRoot();
    const code = runMigrateCommit({ root: tmp });
    expect(code).toBe(1);
    fs.rmSync(tmp, { recursive: true, force: true });
  });

  test('happy path (no flags) reaches commit, no push', async () => {
    const tmp = setupRoot();
    const code = runMigrateCommit({ root: tmp });
    expect(code).toBe(0);
    // push should NOT have been called
    const pushCalls = execFileSync.mock.calls.filter(([, args]) => args && args.includes('push'));
    expect(pushCalls).toHaveLength(0);
    fs.rmSync(tmp, { recursive: true, force: true });
  });

  test('--push flag calls git push', async () => {
    const tmp = setupRoot();
    runMigrateCommit({ root: tmp, push: true });
    const pushCalls = execFileSync.mock.calls.filter(([cmd, args]) => cmd === 'git' && args.includes('push'));
    expect(pushCalls.length).toBeGreaterThan(0);
    fs.rmSync(tmp, { recursive: true, force: true });
  });

  test('--dry prints summary, nothing committed', async () => {
    const tmp = setupRoot();
    const code = runMigrateCommit({ root: tmp, dry: true });
    expect(code).toBe(0);
    const commitCalls = execFileSync.mock.calls.filter(([cmd, args]) => cmd === 'git' && args.includes('commit'));
    expect(commitCalls).toHaveLength(0);
    fs.rmSync(tmp, { recursive: true, force: true });
  });
});
```

- [ ] **Step 2: Run to confirm failure**

```bash
cd /Users/Kamal_Syed/Projects/PlanVisualizer
npx jest tests/unit/memory-commit-orchestrator.test.js --no-coverage 2>&1 | tail -5
```

Expected: module not found.

- [ ] **Step 3: Create `tools/lib/memory-commit-orchestrator.js`**

```js
'use strict';
const fs = require('fs');
const path = require('path');
const { execFileSync } = require('child_process');
const { migrateMemory } = require('./memory-migrator');
const { validateMemory } = require('./memory-validator');
const { patchClaudeMd } = require('./memory-claude-md-patcher');

function runMigrateCommit(opts) {
  const { root, dry = false, push = false, pr = false, noTest = false, force = false } = opts;

  // 1. Pre-flight checks
  const gitStatus = execFileSync('git', ['-C', root, 'status', '--porcelain'], { encoding: 'utf8' }).trim();
  if (gitStatus) {
    console.error('[migrate-commit] Abort: working tree must be clean before migration.');
    return 1;
  }

  const branch = execFileSync('git', ['-C', root, 'symbolic-ref', '--short', 'HEAD'], {
    encoding: 'utf8',
  }).trim();
  if (branch === 'develop' || branch === 'main' || branch === 'master') {
    console.error(`[migrate-commit] Abort: on protected branch '${branch}'. Create a feature branch first.`);
    console.error('[migrate-commit] Suggested: git checkout -b feature/US-0178-memory-migration');
    return 1;
  }

  const memoryPath = path.join(root, 'MEMORY.md');
  if (!fs.existsSync(memoryPath)) {
    console.error('[migrate-commit] Abort: MEMORY.md not found at repo root.');
    return 1;
  }

  const topicsDir = path.join(root, 'docs/memory/topics');
  if (!force && fs.existsSync(topicsDir) && fs.readdirSync(topicsDir).filter((f) => f.endsWith('.md')).length > 0) {
    console.error('[migrate-commit] Abort: memory layout already migrated. Pass --force to re-run.');
    return 1;
  }

  // 2. Migration
  let migrateResult;
  try {
    migrateResult = migrateMemory({ root, dry, force: true });
  } catch (e) {
    console.error('[migrate-commit] Abort: migration failed:', e.message);
    return 1;
  }

  // 3. CLAUDE.md patch
  const claudePath = path.join(root, 'CLAUDE.md');
  let claudeChanged = false;
  if (!fs.existsSync(claudePath)) {
    console.warn('[migrate-commit] Warning: CLAUDE.md not found — skipping patch.');
  } else {
    const original = fs.readFileSync(claudePath, 'utf8');
    let patchResult;
    try {
      patchResult = patchClaudeMd(original);
    } catch (e) {
      console.error('[migrate-commit] Abort:', e.message);
      return 1;
    }
    claudeChanged = patchResult.changed;
    if (!dry && patchResult.changed) {
      fs.writeFileSync(claudePath, patchResult.text);
    }
    if (dry && patchResult.changed) {
      console.log('[migrate-commit] dry-run: CLAUDE.md would be patched.');
    }
  }

  if (dry) {
    const t = (migrateResult.topicFiles || []).length;
    const a = (migrateResult.archiveOps || []).length;
    const o = (migrateResult.lessonOrphans || []).length;
    console.log(
      `[migrate-commit] dry-run: ${t} topic files, ${a} archive ops, ${o} lesson orphans, CLAUDE.md ${claudeChanged ? 'would patch' : 'already patched'}.`,
    );
    return 0;
  }

  // 4. Verification
  if (!noTest) {
    try {
      execFileSync('npx', ['jest', '--no-coverage'], { cwd: root, stdio: 'inherit' });
    } catch {
      console.error('[migrate-commit] Abort: test suite failed. Fix tests before committing.');
      return 1;
    }
  }

  const vResult = validateMemory({ root });
  if (!vResult.ok) {
    console.error('[migrate-commit] Abort: memory validation failed:');
    console.error(vResult.diff);
    return 1;
  }

  // 5. Stage + commit
  const stagePaths = ['docs/memory/', 'MEMORY.md', 'CLAUDE.md', 'docs/LESSONS.md', 'docs/ID_REGISTRY.md'];
  execFileSync('git', ['-C', root, 'add', ...stagePaths], { stdio: 'ignore' });

  const tf = migrateResult.topicFiles || [];
  const topicCount = tf.filter((f) => f.category === 'topics').length;
  const sessionCount = tf.filter((f) => f.category === 'sessions').length;
  const snapshotCount = tf.filter((f) => f.category === 'snapshots').length;
  const archiveCount = (migrateResult.archiveOps || []).length;
  const orphanCount = (migrateResult.lessonOrphans || []).length;

  const commitMsg = [
    'chore(memory): bootstrap docs/memory/ layout (US-0175)',
    '',
    'Migration commit produced by `tools/memory.js migrate-commit`.',
    '',
    '- Splits monolithic MEMORY.md into per-topic files under docs/memory/{topics,sessions,snapshots}/',
    '- Rewrites MEMORY.md as compact auto-generated index',
    `- Archives ${archiveCount} superseded snapshots to docs/memory/archive/snapshots/`,
    `- Triages ## Lessons Learned section against docs/LESSONS.md (${orphanCount} orphans appended)`,
    '- Patches CLAUDE.md with new memory layout instructions',
    '',
    'Counts:',
    `- Topic files written: ${topicCount}`,
    `- Session files written: ${sessionCount}`,
    `- Snapshot files written: ${snapshotCount}  (archived: ${archiveCount})`,
    `- Lesson orphans appended: ${orphanCount}`,
    '',
    'Run `node tools/memory.js validate` to confirm MEMORY.md/topic file consistency.',
  ].join('\n');

  try {
    execFileSync('git', ['-C', root, 'commit', '-m', commitMsg], { stdio: 'pipe' });
    console.log('[migrate-commit] Committed migration.');
  } catch (e) {
    console.error('[migrate-commit] Abort: commit failed.');
    return 1;
  }

  // 6. Optional push
  if (push || pr) {
    try {
      execFileSync('git', ['-C', root, 'push', '-u', 'origin', branch], { stdio: 'inherit' });
      console.log(`[migrate-commit] Pushed to origin/${branch}.`);
    } catch {
      console.warn('[migrate-commit] Warning: push failed. Commit landed locally; push manually.');
      return 0;
    }
  }

  // 7. Optional PR
  if (pr) {
    try {
      execFileSync('gh', ['--version'], { stdio: 'ignore' });
    } catch {
      console.warn('[migrate-commit] Warning: gh CLI not found. Create PR manually.');
      return 0;
    }
    let prARef = '';
    try {
      const out = execFileSync(
        'gh',
        ['pr', 'list', '--state', 'merged', '--search', 'feature/US-0175 in:head', '--limit', '1', '--json', 'number'],
        { encoding: 'utf8', cwd: root },
      );
      const parsed = JSON.parse(out);
      if (parsed[0]) prARef = ` (#${parsed[0].number})`;
    } catch {
      /* no match or gh unavailable */
    }

    const prBody = `## Summary

Migration commit produced by tools/memory.js migrate-commit. This is PR B of the US-0175 split: PR A${prARef} shipped the tooling inert; this PR runs it and ships the result.

- Topic files written: ${topicCount}
- Session files written: ${sessionCount}
- Snapshot files written: ${snapshotCount} (archived: ${archiveCount})
- Lesson orphans appended: ${orphanCount}
- CLAUDE.md patched (Mandatory Session Startup + Session Close Checklist)

## Test Plan

- [x] tools/memory.js migrate ran cleanly
- [x] CLAUDE.md patches applied without abort
- [x] Test suite passes
- [x] tools/memory.js validate exits 0
- [ ] Manual: open MEMORY.md and confirm it's the compact index, not the old monolithic file
- [ ] Manual: spot-check 2-3 topic files for verbatim section content
- [ ] Manual: confirm CLAUDE.md instructions read correctly

🤖 Generated with [Claude Code](https://claude.com/claude-code)`;

    try {
      const prUrl = execFileSync(
        'gh',
        [
          'pr',
          'create',
          '--base',
          'develop',
          '--head',
          branch,
          '--title',
          'feat(memory): bootstrap memory layout migration (US-0175)',
          '--body',
          prBody,
        ],
        { encoding: 'utf8', cwd: root },
      ).trim();
      console.log(`[migrate-commit] PR created: ${prUrl}`);
    } catch {
      console.warn('[migrate-commit] Warning: gh pr create failed. Create PR manually.');
    }
  }

  console.log('[migrate-commit] Done.');
  return 0;
}

module.exports = { runMigrateCommit };
```

- [ ] **Step 4: Run tests to confirm they pass**

```bash
npx jest tests/unit/memory-commit-orchestrator.test.js --no-coverage 2>&1 | tail -5
```

Expected: 8 tests pass.

- [ ] **Step 5: Run full suite for regressions**

```bash
npx jest --no-coverage 2>&1 | tail -3
```

Expected: all pass.

- [ ] **Step 6: Commit**

```bash
cd /Users/Kamal_Syed/Projects/PlanVisualizer
git add tools/lib/memory-commit-orchestrator.js tests/unit/memory-commit-orchestrator.test.js
git commit -m "feat(US-0178): add memory-commit-orchestrator.js — 7-step migrate-commit pipeline"
```

---

### Task 3: Wire into `tools/memory.js` + npm script

**Files:**

- Modify: `tools/memory.js`
- Modify: `package.json`

Add `--push`, `--pr`, `--no-test` flag parsing to `parseArgs`. Add `migrate-commit` dispatch. Add npm script.

- [ ] **Step 1: Update `parseArgs` in `tools/memory.js`**

In `tools/memory.js`, find `parseArgs`. The current return is `{ cmd, dry, force, days }`. Expand to:

```js
function parseArgs(argv) {
  const args = argv.slice(2);
  const cmd = args[0] || null;
  let dry = false;
  let force = false;
  let days = null;
  let push = false;
  let pr = false;
  let noTest = false;
  for (let i = 1; i < args.length; i++) {
    if (args[i] === '--dry' || args[i] === '--dry-run') dry = true;
    else if (args[i] === '--force') force = true;
    else if (args[i] === '--push') push = true;
    else if (args[i] === '--pr') {
      pr = true;
      push = true;
    } else if (args[i] === '--no-test') noTest = true;
    else if (args[i] === '--days' && args[i + 1]) {
      days = parseInt(args[i + 1], 10);
      i++;
    }
  }
  return { cmd, dry, force, days, push, pr, noTest };
}
```

- [ ] **Step 2: Update `dispatch` in `tools/memory.js`**

After the existing `if (cmd === 'validate') { ... }` block and before the final usage error, add:

```js
if (cmd === 'migrate-commit') {
  const { runMigrateCommit } = require('./lib/memory-commit-orchestrator');
  return runMigrateCommit({
    root: ROOT,
    dry,
    push,
    pr,
    noTest,
    force,
  });
}
```

- [ ] **Step 3: Update usage error in `dispatch`**

Find:

```js
console.error('Usage: node tools/memory.js {compact|archive|migrate|validate} [--dry] [--force] [--days N]');
```

Replace with:

```js
console.error(
  'Usage: node tools/memory.js {compact|archive|migrate|migrate-commit|validate} [--dry] [--force] [--push] [--pr] [--no-test] [--days N]',
);
```

- [ ] **Step 4: Add npm script to `package.json`**

In `package.json`, in the `scripts` object, add after `memory:validate`:

```json
"memory:migrate-commit": "node tools/memory.js migrate-commit"
```

- [ ] **Step 5: Update `memory-cli.test.js` for new flags**

In `tests/unit/memory-cli.test.js`, add these tests to the existing `describe('parseArgs')` block:

```js
test('--push flag', () => {
  expect(parseArgs(['node', 'memory.js', 'migrate-commit', '--push']).push).toBe(true);
});
test('--pr flag sets both pr and push', () => {
  const r = parseArgs(['node', 'memory.js', 'migrate-commit', '--pr']);
  expect(r.pr).toBe(true);
  expect(r.push).toBe(true);
});
test('--no-test flag', () => {
  expect(parseArgs(['node', 'memory.js', 'migrate-commit', '--no-test']).noTest).toBe(true);
});
```

- [ ] **Step 6: Run tests**

```bash
cd /Users/Kamal_Syed/Projects/PlanVisualizer
npx jest tests/unit/memory-cli.test.js --no-coverage 2>&1 | tail -5
```

Expected: 8 tests pass (5 original + 3 new).

- [ ] **Step 7: Smoke-test CLI**

```bash
cd /Users/Kamal_Syed/Projects/PlanVisualizer
node tools/memory.js migrate-commit --dry 2>&1 | tail -3
```

Expected: `[migrate-commit] Abort: docs/memory/topics/ already exists` (because migration ran in this project). The abort is correct — it proves the pre-flight works. Pass `--force` to override: `node tools/memory.js migrate-commit --force --dry` — should print dry-run summary.

- [ ] **Step 8: Run full test suite**

```bash
npx jest --no-coverage 2>&1 | tail -3
```

Expected: all pass.

- [ ] **Step 9: Commit**

```bash
cd /Users/Kamal_Syed/Projects/PlanVisualizer
git add tools/memory.js package.json tests/unit/memory-cli.test.js
git commit -m "feat(US-0178): wire migrate-commit into tools/memory.js + npm script"
```
